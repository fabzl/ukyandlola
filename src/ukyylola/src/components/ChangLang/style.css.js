import styled from 'styled-components';
import theme from '../../styles/theme';

const ButtonChangLang = styled.a`
	cursor: pointer;
	font-size: 0.7rem;
	z-index: 999;
	color: blue;
`;

export { ButtonChangLang };
